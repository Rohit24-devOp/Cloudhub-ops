const pages=['Dashboard','IAM Security','EC2 Instances','VPC Designer','Security Center','AWS WAF','CloudWatch','Deployments','Cost Estimator','Reports','Notifications','Settings'];
const icons=['▦','♙','▣','⌘','◈','⊞','⌁','↗','$','▤','●','⚙'];
let state={user:null,instances:[],audit:{score:92,findings:[]},waf:{rules:[]},deployments:[],notifications:[]}; let active='Dashboard';
const api=async(path,options={})=>{const res=await fetch(path,{headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const json=await res.json();if(!res.ok)throw new Error(json.error||'Request failed');return json};
const metric=(a,b,c,color='')=>`<article class="metric ${color}"><small>${a}</small><b>${b}</b><span>${c}</span></article>`;
const chart=()=>`<article class="panel chart-panel"><div class="panel-heading"><div><small>LIVE METRICS</small><h3>Infrastructure performance</h3></div><span class="live-pill">● LIVE</span></div><svg viewBox="0 0 800 180" preserveAspectRatio="none"><path class="gridline" d="M0 40H800M0 90H800M0 140H800"/><path class="cpu-line" d="M0 145L80 128L150 142L220 77L290 110L360 50L430 98L500 72L570 105L650 35L720 69L800 42"/><path class="network-line" d="M0 160L80 150L150 158L220 119L290 135L360 103L430 130L500 95L570 117L650 73L720 108L800 81"/></svg></article>`;
const toast=(msg,warning=false)=>{const x=document.createElement('div');x.className='toast';x.textContent=`${warning?'!':'✓'}  ${msg}`;document.body.append(x);setTimeout(()=>x.remove(),3200)};
async function hydrate(){const [dash,audit,waf,deploys,notes]=await Promise.all(['/api/dashboard','/api/iam/audit','/api/waf','/api/deployments','/api/notifications'].map(x=>api(x)));Object.assign(state,{...dash,audit,waf,deployments:deploys,notifications:notes});}
const dashboard=()=>`<div class="page-grid"><div class="welcome"><div><span class="live-pill">● ALL SYSTEMS OPERATIONAL</span><h1>Good morning, ${state.user.name}. <i>Let’s build.</i></h1><p>Live data from your CloudOps Hub workspace.</p></div><button class="push-button launch">+ Launch resource</button></div><section class="metric-grid">${metric('SECURITY SCORE',state.securityScore||state.audit.score,'Latest IAM scan','sage')}${metric('RUNNING EC2',state.running||0,`${state.stopped||0} stopped instances`)}${metric('MONTHLY COST','$8,462','↓ 8.6% from last month','yellow')}${metric('ACTIVE DEPLOYS',state.deployments.length,'API-backed records')}</section><section class="dashboard-split">${chart()}<article class="panel security-card"><small>SECURITY POSTURE</small><div class="score">${state.audit.score}<span>/100</span></div><div class="score-bar"><i></i></div><p><b>Strong posture.</b> ${state.audit.findings.length} actions need review.</p><button class="text-button" data-page="IAM Security">Review IAM audit →</button></article></section></div>`;
const ec2=()=>`<div class="module"><div class="module-head"><div><span class="live-pill">● ${state.instances.length} INSTANCES</span><h1>EC2 Instances</h1><p>Start, stop, restart, and launch compute resources.</p></div><button class="push-button launch">+ Launch instance</button></div><article class="panel table-panel"><table><thead><tr><th>INSTANCE</th><th>TYPE</th><th>REGION</th><th>STATUS</th><th>ACTION</th></tr></thead><tbody>${state.instances.map(x=>`<tr><td><b>${x.name}</b><small>${x.id}</small></td><td>${x.type}</td><td>${x.region}</td><td><span class="status ${x.status==='Running'?'on':'off'}">● ${x.status}</span></td><td><button class="table-action" data-action="${x.status==='Running'?'stop':'start'}" data-id="${x.id}">${x.status==='Running'?'Stop':'Start'}</button></td></tr>`).join('')}</tbody></table></article></div>`;
const iam=()=>`<div class="module"><div class="module-head"><div><span class="live-pill">● AUDIT COMPLETE</span><h1>IAM Security Audit</h1><p>Persistent audit history and actionable access-control findings.</p></div><button class="push-button" id="run-audit">↻ Run new audit</button></div><section class="metric-grid">${metric('SECURITY SCORE',state.audit.score,'Updated from API','sage')}${metric('IAM USERS','48','43 protected with MFA')}${metric('RISK FINDINGS',String(state.audit.findings.length).padStart(2,'0'),'Requires action','yellow')}${metric('ACCESS KEYS','11','1 unused key found')}</section><article class="panel activity-panel"><div class="panel-heading"><div><small>PRIORITIZED FINDINGS</small><h3>Fix these first</h3></div></div><ul>${state.audit.findings.map(f=>`<li><i class="yellow-dot"></i><div><b>${f.title}</b><span>${f.level} risk · ${f.detail}</span></div><em>→</em></li>`).join('')}</ul></article></div>`;
const vpc=()=>`<div class="module"><div class="module-head"><div><span class="live-pill">● ${state.vpcs?.length||1} VPCS DISCOVERED</span><h1>VPC Designer</h1><p>Design secure public/private network topology before deployment.</p></div><button class="push-button" id="create-vpc">+ Create VPC</button></div><article class="panel vpc-canvas"><div class="vpc-toolbar"><button>↖ Select</button><button>＋ Subnet</button><button>＋ Gateway</button><span>Topology saved to the CloudOps API</span></div><div class="network-map"><div class="vpc-box"><b>VPC · production-network</b><small>10.0.0.0/16</small><div class="subnet public"><strong>PUBLIC SUBNET</strong><span>10.0.1.0/24</span><i>◉</i></div><div class="subnet private"><strong>PRIVATE SUBNET</strong><span>10.0.11.0/24</span><i>▣</i></div><div class="gateway">⇄<small>Internet Gateway</small></div></div></div></article></div>`;
const waf=()=>`<div class="module"><div class="module-head"><div><span class="live-pill">● WEB ACL ACTIVE</span><h1>Cloud Security — WAF</h1><p>Manage Web ACL controls and view attack protection analytics.</p></div><button class="push-button" id="save-waf">Save Web ACL</button></div><section class="metric-grid">${metric('REQUESTS BLOCKED',state.waf.blocked,'Last 24 hours','yellow')}${metric('ACTIVE RULES',state.waf.rules.length,'AWS managed + custom','sage')}${metric('THREATS DETECTED','17','No critical threats')}${metric('ALLOWED','38.4k','99.7% legitimate')}</section><section class="waf-layout"><article class="panel rules"><div class="panel-heading"><div><small>WEB ACL RULES</small><h3>${state.waf.name}</h3></div></div>${state.waf.rules.map(x=>`<label><input checked type="checkbox" value="${x}"> ${x}<span>Active protection rule</span></label>`).join('')}</article><article class="panel attack-analytics"><small>ATTACK ANALYTICS</small><h3>Top blocked categories</h3>${['SQL injection','Bad bots','Rate limit','XSS attempts'].map((x,i)=>`<div class="attack-bar"><span>${x}</span><i style="width:${83-i*19}%"></i><b>${542-i*120}</b></div>`).join('')}</article></section></div>`;
const deployments=()=>`<div class="module"><div class="module-head"><div><span class="live-pill">● GITHUB CONNECTED</span><h1>Deployment Center</h1><p>Trigger and track CI/CD deployment records through the API.</p></div><button class="push-button" id="deploy">+ New deployment</button></div><article class="panel pipeline"><small>LATEST DEPLOYMENT</small><h3>${state.deployments[0]?.service||'production-api'} <span>${state.deployments[0]?.branch||'main'}</span></h3><div class="pipeline-steps"><div class="done">✓<b>Build</b><small>Passed</small></div><div class="done">✓<b>Test</b><small>Passed</small></div><div class="current">↗<b>Deploy</b><small>${state.deployments[0]?.status||'Ready'}</small></div><div>4<b>Verify</b><small>pending</small></div></div></article></div>`;
const generic=()=>`<div class="module"><div class="module-head"><div><span class="live-pill">● CONNECTED</span><h1>${active}</h1><p>This module is connected to the authenticated CloudOps Hub API.</p></div></div><section class="metric-grid">${metric('HEALTH','Healthy','Live data ready','sage')}${metric('RESOURCES','24','Updated just now')}${metric('LAST SYNC','2 min','AWS connector ready','yellow')}${metric('ALERTS','0','No action required')}</section>${chart()}</div>`;
function render(){document.querySelectorAll('[data-page]').forEach(x=>x.classList.toggle('selected',x.dataset.page===active));document.querySelector('#page-title').textContent=active;document.querySelector('#crumb').textContent=active==='Dashboard'?'OVERVIEW':`PLATFORM / ${active.toUpperCase()}`;document.querySelector('#page-view').innerHTML=active==='Dashboard'?dashboard():active==='EC2 Instances'?ec2():active==='IAM Security'?iam():active==='VPC Designer'?vpc():active==='AWS WAF'?waf():active==='Deployments'?deployments():generic();bind();}
function bind(){document.querySelectorAll('[data-page]').forEach(x=>x.onclick=()=>{active=x.dataset.page;render()});document.querySelectorAll('.launch').forEach(x=>x.onclick=launch);document.querySelectorAll('[data-action]').forEach(x=>x.onclick=async()=>{try{await api(`/api/instances/${x.dataset.id}/${x.dataset.action}`,{method:'POST'});await hydrate();render();}catch(e){toast(e.message,true)}});document.querySelector('#run-audit')?.addEventListener('click',async()=>{await api('/api/iam/audit',{method:'POST'});await hydrate();render();toast('IAM audit completed.')});document.querySelector('#create-vpc')?.addEventListener('click',async()=>{await api('/api/vpcs',{method:'POST',body:JSON.stringify({name:'application-network'})});toast('VPC design saved.')});document.querySelector('#save-waf')?.addEventListener('click',async()=>{await api('/api/waf',{method:'PUT',body:JSON.stringify({rules:[...document.querySelectorAll('.rules input:checked')].map(x=>x.value)})});await hydrate();toast('Web ACL saved.')});document.querySelector('#deploy')?.addEventListener('click',async()=>{await api('/api/deployments',{method:'POST',body:'{}'});await hydrate();render();toast('Deployment started.')})}
function launch(){const modal=document.createElement('div');modal.className='modal-backdrop';modal.innerHTML=`<form class="modal"><button class="close" type="button">×</button><small>NEW RESOURCE</small><h2>Launch an EC2 instance</h2><label>Name<input required autofocus placeholder="payments-api"></label><label>Type<select><option>t3.medium</option><option>t3.large</option></select></label><button class="push-button">Launch instance →</button></form>`;document.body.append(modal);modal.querySelector('.close').onclick=()=>modal.remove();modal.querySelector('form').onsubmit=async e=>{e.preventDefault();try{await api('/api/instances',{method:'POST',body:JSON.stringify({name:e.currentTarget.querySelector('input').value,type:e.currentTarget.querySelector('select').value})});modal.remove();await hydrate();active='EC2 Instances';render();toast('Instance provisioning started.')}catch(err){toast(err.message,true)}}}
async function openApp(){try{await hydrate();document.querySelector('.site-header').classList.add('hidden');document.querySelector('body > main').classList.add('hidden');document.querySelector('.site-footer').classList.add('hidden');app.classList.remove('hidden');render()}catch(e){toast(e.message,true)}}
const app=document.createElement('section');app.id='application';app.className='application hidden';app.innerHTML=`<aside class="app-sidebar"><a class="brand" href="/"><span class="brand-mark">↯</span><span>CLOUDOPS<br>HUB</span></a><small>WORKSPACE</small><nav class="app-nav">${pages.map((x,i)=>`<button data-page="${x}"><i>${icons[i]}</i>${x}</button>`).join('')}</nav><button class="logout">← Log out</button><div class="built-by">BUILT WITH <b>CODTECH<br>IT SOLUTIONS</b></div></aside><div class="app-content"><header class="app-header"><button class="sidebar-toggle">☰</button><div><small id="crumb">OVERVIEW</small><h2 id="page-title">Dashboard</h2></div><div class="top-tools"><button class="bell">●<b>3</b></button><button class="theme">◐</button><button class="profile">CO</button></div></header><main id="page-view"></main></div>`;document.body.append(app);
function auth(){const modal=document.createElement('div');modal.className='modal-backdrop';modal.innerHTML=`<form class="modal auth-modal"><span class="brand-mark">↯</span><small>WELCOME TO CLOUDOPS HUB</small><h2>Start your cloud workspace.</h2><label>Work email<input name="email" type="email" required placeholder="you@company.com"></label><label>Password<input name="password" type="password" minlength="8" required placeholder="8 or more characters"></label><button class="push-button">Continue to dashboard →</button><p>New here? Your account is created automatically.</p></form>`;document.body.append(modal);modal.querySelector('form').onsubmit=async e=>{e.preventDefault();const values=Object.fromEntries(new FormData(e.currentTarget));try{try{state.user=(await api('/api/auth/login',{method:'POST',body:JSON.stringify(values)})).user}catch{state.user=(await api('/api/auth/register',{method:'POST',body:JSON.stringify(values)})).user}modal.remove();await openApp()}catch(err){toast(err.message,true)}}}
document.querySelectorAll('[data-auth],.login,.price-card .push-button').forEach(x=>x.onclick=e=>{e.preventDefault();auth()});app.querySelector('.logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'});app.classList.add('hidden');document.querySelector('.site-header').classList.remove('hidden');document.querySelector('body > main').classList.remove('hidden');document.querySelector('.site-footer').classList.remove('hidden')};app.querySelector('.sidebar-toggle').onclick=()=>app.classList.toggle('sidebar-open');app.querySelector('.theme').onclick=()=>app.classList.toggle('app-light');app.querySelector('.bell').onclick=()=>{active='Notifications';render()};
